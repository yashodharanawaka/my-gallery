version: 0.2

phases:
  pre_build:
    commands:
      - node -v
      - npm -v

  install:
    commands:
      - npm install

  build:
    commands:
      - npm run build

  post_build:
    commands:
      - aws cloudformation deploy --template-file s3-deployment-template.yml --stack-name MyS3BucketStack --capabilities CAPABILITY_NAMED_IAM
